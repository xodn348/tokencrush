"""Token compression engine."""


class Compressor:
    """Handles prompt compression using LLMlingua."""

    def compress(self, prompt: str) -> str:
        """Compress a prompt to reduce token count."""
        return prompt
